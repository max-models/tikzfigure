---
title: TikzFigure
description: A Python library to generate readable TikZ figures
hero:
  tagline: Build TikZ figures programmatically with Python
  actions:
    - text: Get Started
      link: /tikzfigure/installation/
      icon: right-arrow
    - text: View on GitHub
      link: https://github.com/max-models/tikzfigure
      icon: external
      variant: minimal
---

[Check out the first tutorial →](/tikzfigure/tutorials/tutorial_01_getting_started/)

---

import { Card, CardGrid, Tabs, TabItem } from "@astrojs/starlight/components";

# Quickstart

**Requirements:** Python 3.9+, a LaTeX distribution with [`pdflatex`](https://gprivate.com/6khew)

```bash
pip install tikzfigure
```

```python
from tikzfigure import TikzFigure

fig = TikzFigure()
a = fig.node(x = 0, y = 0, content="A", shape="circle", fill="cyan!40")
b = fig.node(x = 3, y = 0, content="B", shape="circle", fill="cyan!40")
c = fig.node(x = 0, y = -2, content="C", shape="circle", fill="cyan!40")

fig.draw([a, b], arrows="->")
fig.draw([b, c], arrows="->")

fig.show()
```

![Quickstart output](/tikzfigure/quickstart/output.png)

This is the generated Tikz code (`str(fig)`):

```tex
% --------------------------------------------- %
% Tikzfigure generated by tikzfigure v0.2.1     %
% https://github.com/max-models/tikzfigure      %
% --------------------------------------------- %
\begin{tikzpicture}
    \node[shape=circle, fill=cyan!40] (node0) at ({0}, {0}) {A};
    \node[shape=circle, fill=cyan!40] (node1) at ({3}, {0}) {B};
    \node[shape=circle, fill=cyan!40] (node2) at ({0}, {-2}) {C};
    \draw[arrows=->] (node0) to (node1);
    \draw[arrows=->] (node1) to (node2);
\end{tikzpicture}
```

{/*
## Features

<CardGrid stagger>
  <Card title="Python API" icon="pencil">
    Build TikZ figures via a clean Python API — no LaTeX knowledge required.
  </Card>
  <Card title="Multiple export formats" icon="document">
    Export to LaTeX source, PDF, PNG, or JPG.
  </Card>
  <Card title="Layer support" icon="list-format">
    Z-ordering via named layers using `pgfonlayer`.
  </Card>
  <Card title="3D figures" icon="star">
    3D figure support via `pgfplots`.
  </Card>
  <Card title="Jupyter magic" icon="laptop">
    Inline rendering in Jupyter/IPython with `%%tikz`.
  </Card>
  <Card title="Readable output" icon="open-book">
    Inspectable, human-readable LaTeX output.
  </Card>
</CardGrid>
*/}
