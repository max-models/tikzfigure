[pytest]
pythonpath = src
testpaths = src/tikzfigure/tests
markers =
    network: tests that make real network requests (deselect with -m "not network")
    latex: tests that compile documents with pdflatex (deselect with -m "not latex")
